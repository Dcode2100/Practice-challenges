console.log("operators in JS")


let a = 10;
let b = 2;
console.log(" a+b ", a+b)
console.log("a ** b", a**b) // exponential operators.
console.log(" a%b ", a%b)   // modular operator.
console.log(" a++ =  ", a++)  //incrementor.
console.log(" a+b ", a+b)

// Comparison operator
let a1 = 22
let a2 = "22"
console.log("a != b is ", a!=b ) // Not equal to 
console.log("a === b is ", a===b ) // Checks if the number are same along with the type in this condition NO the type isn't same.
console.log("a !== b is ", a!==b ) // check if the a = b and type is different.

// type operators
let a4 = 22;
console.log(typeof(a1)); // return the type of variable
console.log(instanceof(a1));   // Returns true if an object is an instance of an object type
